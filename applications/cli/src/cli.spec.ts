describe("CLI", () => {
  it("works", () => {})
  // create an account
  // record a transaction
  // record a transfer
  // view balances

  // create a saving target
  // view budgets
  // view runway
})
