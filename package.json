{
  "name": "budgie",
  "version": "1.0.0",
  "description": "Personal budgeting tool",
  "main": "applications/cli/dist/index.js",
  "scripts": {
    "test": "jest",
    "publish": "yarn workspace @budgie/cli bundle && rm -rf bundle/ && unzip -q ./applications/cli/bundle.zip"
  },
  "author": "cedarwards",
  "license": "UNLICENSED",
  "workspaces": [
    "applications/*",
    "planning",
    "fs-plugin"
  ],
  "devDependencies": {
    "@types/jest": "^26.0.23"
  }
}
